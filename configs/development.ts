import type { AppConfig } from 'cfg';

const config: AppConfig = {
    langs: ['ru'],
    render: {
        isHot: true
    },

    static: {
        baseUrl: '/static/',
        version: ''
    }
};

export default config;
