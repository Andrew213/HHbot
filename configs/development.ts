import type { AppConfig } from 'cfg';

const config: AppConfig = {
    langs: ['ru'],
    render: {
        isHot: true
    },

    static: {
        baseUrl: '/static/'
    }
};

export default config;
